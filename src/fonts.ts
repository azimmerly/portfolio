import { Inter, Nunito } from "next/font/google";

export const interFont = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const nunitoFont = Nunito({
  subsets: ["latin"],
  display: "swap",
});
